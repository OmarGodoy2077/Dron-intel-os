# Smart-Swarm Neuro-Simbolico -- Python Dependencies
#
# DEPENDENCIA CRITICA EXTERNA: SWI-Prolog (requerido por pyswip)
#
#   Windows : winget install SWI-Prolog.SWI-Prolog
#             (o descargar desde https://www.swi-prolog.org/download/stable)
#   Linux   : sudo apt install swi-prolog
#   macOS   : brew install swi-prolog
#
#   Despues de instalar SWI-Prolog, instalar este requirements.txt:
#   pip install -r requirements.txt

# Deep Learning
torch>=2.3.0
torchvision>=0.18.0

# Reinforcement Learning
gymnasium>=0.29.1

# Symbolic AI - Prolog bridge (requiere SWI-Prolog instalado en el sistema)
pyswip>=0.2.10

# Web API & Real-time
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
websockets>=12.0
python-multipart>=0.0.9

# Data Science
pandas>=2.2.0
numpy>=1.26.0
scikit-learn>=1.5.0
matplotlib>=3.9.0
seaborn>=0.13.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.7.0
scipy>=1.13.0

# Testing (suite en /tests — ejecutar `pytest` desde la raíz del proyecto)
pytest>=8.0.0
pytest-cov>=5.0.0
